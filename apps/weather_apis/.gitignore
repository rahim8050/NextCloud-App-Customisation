/vendor/
/node_modules/
/.php-cs-fixer.cache
/.phpunit.result.cache
/.psalm-cache/

# Root .gitignore ignores *.lock; keep this app's lockfile tracked
!composer.lock

# local tooling artifacts (not part of repo)
../../netstat
../../wg

# AI prompts (local workspace only)
prompts/