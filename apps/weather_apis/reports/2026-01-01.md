# Daily Progress — Thu, Jan 1, 2026 (EAT)

## Done
- Checked Nextcloud app config via `occ config:list --private weather_apis` and confirmed **mixed legacy + new keys** (e.g. `api_key/base_url/dev_*` alongside `apiKey/baseUrl/clientId/...`).
- Verified **runtime mismatch**: `AppConfig` getters fail with:
  - `API key could not be decrypted.`
  - `HMAC secret could not be decrypted.`
- Confirmed DRF expects **`wk_live_...`** style API keys (auth code + your earlier key prefix check), while the value currently in config looked like a long non-`wk_live_` string (len ~260), so formats are inconsistent.
- Drafted the Codex direction: **canonical schema + safe migration + secret rotation window + ensure admin-save == runtime-read**.

## In progress
- Making Nextcloud Admin UI save **canonical keys** and ensuring runtime reads the *same* values.
- Aligning secret storage so `apiKey` and `hmacSecret` are stored/retrieved consistently (target: `plain:v1:<secret>` or whatever your `AppConfig` expects—right now it’s failing).

## Next
- Implement Nextcloud migration mapping:
  - `api_key → apiKey`
  - `base_url → baseUrl`
  - `dev_allow_insecure_local_http → devAllowHttp`
  - `dev_allowlist_hosts → allowlistHosts`
  - plus any `clientId/signingSecret` normalization you chose as canonical
- Fix decryption failures by ensuring stored values match what `getApiKey()` / `getHmacSecret()` can decode.
- Expose/verify DRF integration endpoints end-to-end:
  - `/api/v1/integrations/token/`
  - `/api/v1/integrations/whoami/`
  - `/api/v1/integrations/nextcloud/ping/`
  and wire Nextcloud controller/routes to call them; prove with one curl.

## Blockers
- **Hard blocker:** Nextcloud can’t complete HMAC handshake testing while `apiKey`/`hmacSecret` retrieval fails (decryption stops the chain).
- **Config ambiguity:** the “API key” in Nextcloud config doesn’t match DRF’s expected `wk_live_...` format, so auth may fail even after storage is fixed.

## Links / Evidence
- Nextcloud config dump:
  - `sudo -u www-data php /var/www/html/nextcloud/occ config:list --private weather_apis`
- Nextcloud runtime getter test:
  - one-liner calling `\OCA\WeatherApis\Service\AppConfig::getApiKey()` and `getHmacSecret()`
- DRF source:
  - `api_keys/auth.py` (shows `API_KEY_PREFIX = "wk_live_"` and hashing/pepper flow)

