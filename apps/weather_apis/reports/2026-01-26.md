# Daily Report — 2026-01-26 (2 a.m drills)

## Done
- Admin settings UI now uses full right-pane width on wide screens (responsive grid layout; scoped to `weather_apis`; no HTML/JS wiring changes).
- Fixed “Dev: allow insecure local HTTP” checkbox appearing oversized by excluding `checkbox/radio/hidden/submit` from the input styling rules and adding an explicit checkbox reset in `css/admin-settings.css`.
- NDVI UI output moved from raw JSON dumps to human-readable cards with an optional collapsed “Debug” section (DOM-safe rendering).
- NDVI success handling aligned to accept `status === 0` as OK (kept compatibility with `ok === true` / `status === 'ok'` patterns where applicable).
- Verified visually (screenshots): farms table left + NDVI panel right dashboard layout is working; Save button spans full width; checkbox no longer a giant purple block.

## In progress
- Human-readable normalization across all NDVI actions (latest / timeseries / queue / refresh / raster) so common fields are always surfaced consistently (engine, cached/stale, lookback_days, date range, etc.).
- UI polish: ensure the “Dev: allow insecure local HTTP” row matches Nextcloud control styling (no custom padding/width leaking into checkbox container).

## Next
- NDVI messaging rules:
  - Display a badge for `cached`/`stale` states (e.g., “Cached” + “Stale”) and a short hint when `stale: true`.
  - Timeseries empty-state: keep “No observations for the selected range” as the primary message, with Debug collapsed by default.
- Raster preview reliability check:
  - Confirm PNG route works consistently (no 412/CSRF surprises) and preview loads as blob + object URL.
- Run local quality gate if needed: `composer run gate`.

## Blockers / Risks
- Backend payload shape variance (`status: 0` vs `{status:'ok'}` vs nested `data`) can cause UI regressions if handlers don’t normalize responses consistently.
- CSS selector support (e.g., any use of advanced selectors) could differ across environments; keep selectors conservative and scoped.

## Links
- Repo: nextcloud/apps/weather_apis
- Files touched:
  - `css/admin-settings.css`
  - `js/admin-settings.js`
- Evidence: screenshots captured (wide layout, farms dashboard layout, checkbox fixed, NDVI readable cards with Debug).

