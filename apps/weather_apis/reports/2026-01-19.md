
# Daily Progress — 2026-01-19

## Done
- Verified DRF is reachable from Nextcloud runtime:
  - `sudo -u www-data curl -v http://127.0.0.1:8001/api/schema/?format=json -o /dev/null` → **HTTP 200**
- Collected Nextcloud logs showing base URL validation passes (http + allowlist + local access allowed).
- Identified Farms admin proxy failures:
  - `list farms` previously returning **503** with `code=backend_unavailable` / `reason=request_failed`.
  - UI `create farm` failing with `code=invalid_argument` (due to empty payload when modal has 0 inputs).
- Confirmed **E2E create works** via Nextcloud proxy when sending a valid payload from browser console:
  - `POST /apps/weather_apis/api/v1/admin/farms/create` with `{ "name": "Test Farm" }` → `{ ok: true, message: "Farm created." }`
- Confirmed DRF schema endpoint returns **200** consistently.

## In progress
- Farms Admin UI still renders **0 inputs** in the Create modal (`#weather-apis-farms-modal-fields input` length = 0), causing UI create to submit `{}` and fail “missing required field: name”.

## Next
- Fix Farms schema parsing in Nextcloud UI to handle nested schema shape:
  - `payload.data.schema.schema.fields` vs `payload.data.schema.fields`
- Ensure farms list proxy calls DRF **GET with query params** (no GET body).
- Add/keep underlying transport exception logging in `WeatherApiClient` for `request_failed`.
- Re-test E2E from UI:
  - Refresh farms renders fields + rows
  - Create farm works from modal
  - NDVI actions still functional

## Blockers
- Schema/list issues prevent Farms UI from rendering required fields.
- Transport exception details for list failures are still not surfaced (currently collapsed into `request_failed`).

## Links
- https://github.com/rahim8050/NextCloud-App-Customisation/commit/b646b7557a332809b5f516c5a4341f2f79c7a3ca
