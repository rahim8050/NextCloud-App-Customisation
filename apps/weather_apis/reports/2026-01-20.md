# Daily Progress Report — 2026-01-20 (EAT)

## Done
- Traced “admin farms UI not responsive” to backend + frontend causes:
  - Backend side: `admin/farms/list` failing because token mint call to DRF returned a 4xx (`Guzzle ClientException`) → UI had nothing to render.
  - Mitigation: restarted DRF server; subsequent admin calls started working again.
- Diagnosed Nextcloud-side 500/Internal Server Error events tied to JS combining/cache (`JSCombiner` / `GenericFileException`) and confirmed they’re core asset/cache related (not app logic).
- Confirmed the modal inputs are actually interactive at DOM level (focus works; `el.value` updates via DevTools).
- Found a theme/CSS bug making inputs appear “dead”:
  - Computed `color` and `backgroundColor` both `rgb(235,235,235)` → typed text/caret invisible (not actually unresponsive).
- Identified a layout bug in the “New farm” modal:
  - Modal/card not constrained to viewport → form overflows Nextcloud settings UI and lacks proper internal scrolling.
- Prepared a precise Codex plan to fix modal responsiveness (viewport max-width/max-height + scrollable modal body; namespaced CSS).

## In progress
- Nextcloud UI polish for Farms modal:
  - Add proper modal sizing and internal scroll container.
  - Ensure fields don’t overflow and remain usable across common resolutions.
- Form submit reliability:
  - Inputs currently use `data-field-name`; ensure `name="<field>"` is set if payload is built via `FormData(form)` to avoid `{}` submissions.

## Next
- Apply modal fix in Nextcloud app:
  - `position: fixed` overlay, centered modal.
  - `width: min(720px, calc(100vw - 48px))`
  - `max-height: calc(100vh - 96px)` and scrollable `.weather-apis-farms__modal-body`.
- Add/confirm `name` attributes for all dynamic fields and verify Create Farm sends correct payload.
- Stabilize DRF in dev:
  - run via systemd/Docker restart policy so “backend_unavailable” doesn’t recur.
- Verify end-to-end:
  - Refresh farms → list renders.
  - Create farm → persists and appears in list.
  - NDVI preview/PNG fetch behavior (412/CSRF) after core flow is stable.

## Blockers / Risks
- DRF process downtime causes Nextcloud admin proxy calls to fail (token mint is a hard dependency).
- Nextcloud asset cache/appdata permissions can trigger 500s (JSCombiner) and mask app-level issues until corrected.
- Modal UX currently degrades usability (overflow) until CSS/layout patch lands.

## Links
- DRF repo: https://github.com/rahim8050/weather-apis
- Nextcloud app repo: https://github.com/rahim8050/NextCloud-App-Customisation

