# Daily Report — Nextcloud App (weather_apis)
**Date:** 2026-01-09 (EAT)

## Done
- Confirmed the admin-side flow hits the DRF integration endpoint and can produce a deterministic failure when headers are absent/mismatched.
- Cross-checked that DRF is returning a structured, UI-friendly error contract that Nextcloud can display safely.

## In progress
- Aligning Nextcloud outbound request formation with DRF verification expectations:
  - HTTP method correctness (POST vs GET where relevant)
  - Exact header set and names
  - Canonical request string consistency (path/query/body handling)

## Next
- Ensure the admin “Test connection” UI triggers the correct endpoint and uses `WeatherApiClient` for outbound HTTP (single place to centralize signing + transport).
- Add/adjust unit tests on Nextcloud side to lock:
  - Signing inputs (client id, secret decode behavior, canonical string)
  - Required headers always present on outbound calls
- Document the final shared HMAC contract in a single spec file mirrored in both repos.

## Blockers
- Potential divergence in canonicalization/signing rules between Nextcloud and DRF (even a tiny mismatch breaks signatures).
- Confusion introduced by mixed legacy config paths or multiple sources of truth for client id/secret.

## Links
- DRF rejection indicates missing Nextcloud HMAC headers (`code=missing_headers`) — focus on outbound header population first.

