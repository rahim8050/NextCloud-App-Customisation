# Daily Progress Report — 2026-01-24

## Done
- Mapped out today’s bugs across Nextcloud ↔ DRF integration (no coding; observation + evidence capture).
- Confirmed **Bug #1 is solved**.
- Collected supporting evidence from logs/grep for the `weather_apis` requests to back Monday’s drill notes.
- Noted the current failure pattern: Farms admin UI actions not behaving as expected when buttons are clicked (symptom captured).

## In Progress
- Consolidating all observed symptoms into a single Monday 2:00am drill runbook (what to verify first, what signals confirm/deny each hypothesis).

## Next
- Monday 2:00am drill: replay the exact UI flows and correlate with logs to isolate where the request/response chain breaks (UI → Nextcloud routes → DRF).
- Re-validate Farms schema fetch + list + create path end-to-end with concrete payloads and responses (golden vector approach).
- Re-check NDVI-related routes behavior against auth/CSRF expectations and confirm the expected request headers/tokens.

## Blockers
- Farms admin UI is not reliably executing expected actions when interacting with the buttons (currently symptom-level; needs Monday drill to pinpoint cause).
- Some Farms/NDVI flows appear dependent on upstream responses behaving correctly (status/response shape issues likely influencing UI rendering).

## Links / Evidence
- Local Nextcloud log evidence:
  - `sudo -u www-data grep -n "ccddf8ea-72d9-4223-bd47-a1db81cbef56" /var/www/html/nextcloud/data/nextcloud.log | tail -n 50`
- Screenshot evidence captured in chat (Farms/NDVI UI + current behavior).

