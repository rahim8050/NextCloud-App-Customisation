# Daily Report — 2026-05-26

## Summary
Added `externalFarmId` extraction and injection into NDVI, weather current, and farm state proxy requests in `AdminFarmsController` so the Django backend receives the correct farm identifier for data scoping.

## Changes

### Nextcloud App (`weather_apis`)
- **NDVI latest proxy**: Extracts `externalFarmId` from request params and appends it to query params before forwarding to the backend.
- **NDVI time-series proxy**: Same externalFarmId handling added to the start/end date query flow.
- **Weather current proxy**: Extracts and appends externalFarmId to the weather current request.
- **Farm state proxy**: Extracts and appends externalFarmId to the farm state request.

## Files Modified
| File | Changes |
|------|---------|
| `lib/Controller/AdminFarmsController.php` | +8 lines — externalFarmId handling in 4 proxy methods |

## Stats
- 1 file changed: +8 / -0 lines
- 1 commit

## Known Issues
- None currently blocking

## Next Steps
- Run full gate (`composer run gate`)
