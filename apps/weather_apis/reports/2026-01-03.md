# Daily Report — Nextcloud App (weather_apis)
**Date:** 2026-01-03 (EAT)

## Done
- Completed integration hardening work and documented the audit in `docs/audit_integration.md`.
- Implemented/validated security + reliability improvements:
  - Canonical HMAC signing aligned with DRF spec (method uppercasing, strict path, query canonicalization, raw body SHA-256).
  - Golden vector coverage to prevent future canonicalization drift.
  - Token cache now respects backend `expires_in` and performs a single safe retry on 401 after cache clear.
  - Log redaction/sanitization applied to settings and request handling paths.
  - SSRF/dev allowlist enforcement strengthened with additional test coverage.
- Gates confirmed green (PHP side):
  - `composer run lint` ✅
  - `composer run cs:check` ✅
  - `composer run psalm` ✅
  - `composer run test` ✅

## In progress
- Preparing clean commit(s) for Nextcloud repo (ensuring only intended files are staged).

## Next
- Commit Nextcloud hardening changes with an audit-aligned message.
- Push Nextcloud commits to remote.
- Manual end-to-end validation against DRF:
  - Token bootstrap + whoami
  - Nonce replay returns 403 with `errors.code = "nonce_replay"`
  - Throttle returns 429 + `Retry-After` (using fresh nonce/signature per request)

## Blockers
- None.

## Links
- Local repo: `/var/www/html/nextcloud/apps/weather_apis`
- Audit doc: `docs/audit_integration.md`
- Key components:
  - `lib/Service/TokenSigner.php`
  - `lib/Service/WeatherApiClient.php`
  - `lib/Service/LogSanitizer.php`
- Gate commands:
  - `composer run lint && composer run cs:check && composer run psalm && composer run test`
# Daily Report — Nextcloud App (weather_apis)
**Date:** 2026-01-03 (EAT)

## Done
- Completed integration hardening work and documented the audit in `docs/audit_integration.md`.
- Implemented/validated security + reliability improvements:
  - Canonical HMAC signing aligned with DRF spec (method uppercasing, strict path, query canonicalization, raw body SHA-256).
  - Golden vector coverage to prevent future canonicalization drift.
  - Token cache now respects backend `expires_in` and performs a single safe retry on 401 after cache clear.
  - Log redaction/sanitization applied to settings and request handling paths.
  - SSRF/dev allowlist enforcement strengthened with additional test coverage.
- Gates confirmed green (PHP side):
  - `composer run lint` ✅
  - `composer run cs:check` ✅
  - `composer run psalm` ✅
  - `composer run test` ✅

## In progress
- Preparing clean commit(s) for Nextcloud repo (ensuring only intended files are staged).

## Next
- Commit Nextcloud hardening changes with an audit-aligned message.
- Push Nextcloud commits to remote.
- Manual end-to-end validation against DRF:
  - Token bootstrap + whoami
  - Nonce replay returns 403 with `errors.code = "nonce_replay"`
  - Throttle returns 429 + `Retry-After` (using fresh nonce/signature per request)

## Blockers
- None.

## Links
- Local repo: `/var/www/html/nextcloud/apps/weather_apis`
- Audit doc: `docs/audit_integration.md`
- Key components:
  - `lib/Service/TokenSigner.php`
  - `lib/Service/WeatherApiClient.php`
  - `lib/Service/LogSanitizer.php`
- Gate commands:
  - `composer run lint && composer run cs:check && composer run psalm && composer run test`

