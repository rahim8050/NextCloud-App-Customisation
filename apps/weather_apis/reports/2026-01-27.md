# Daily Report — 2026-01-27 (EAT)

## Done
- Fixed **integration token reuse** (cache behavior now correct; no unnecessary token churn).
- Fixed **schema fetch + parsing** (schema now loads and UI receives the expected fields reliably).
- Unblocked Farms admin baseline (no longer failing due to token/schema).

## In progress
- **VPN build track (WireGuard)**:
  - Locked in approach: WireGuard as separate **UDP** service (default 51820/udp) alongside existing **Nginx :443 TLS** stack (no conflict).
  - Drafted the “golden path” steps: server packages + IP forwarding + keys + `wg0` config + firewall + NAT + client profiles.
- Farms UI end-to-end verification: ensure DRF farms (#1–#18) appear in Nextcloud UI consistently (not only Nextcloud-created farms).
- NDVI preview stability: investigate **PNG 412** (likely CSRF/requesttoken handling on preview route).

## Next
- VPN (WireGuard):
  - Implement server: install wireguard, enable forwarding, generate keys, bring up `wg0`, open UDP port.
  - Add first client profile (phone/laptop), connect, and validate reachability to private services.
  - Decide routing mode: **split tunnel** (VPN subnet only) vs **full tunnel** (all traffic via VPN) based on your use-case.
- Farms:
  - Confirm list proxy uses **GET with query params** (no GET body) and UI renders full remote list.
  - Validate Create/Edit submits populated fields post-schema fix.
- NDVI:
  - Fix 412 by making PNG route CSRF-exempt (`NoCSRFRequired`) or fetching as blob with requesttoken + object URL.

## Blockers
- None for token/schema (cleared).
- Remaining risk: NDVI PNG 412 until route/token handling is patched and verified.

## Links
- DRF schema: `/api/schema/?format=json`
- Nextcloud routes: `/apps/weather_apis/api/v1/admin/farms/...`
- VPN target: `wg0` (WireGuard), UDP 51820, private subnet (e.g., `10.x.x.x/24`)

