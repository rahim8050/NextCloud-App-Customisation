# Nextcloud — Progress Note (2025-12-31)

## Done
- Identified Psalm issues blocking clean static analysis:
  - `RedundantCast` warnings in `lib/Service/WeatherApiClient.php` around parsing scheme/host/port.
- Prepared for HMAC → DRF `whoami` validation from Nextcloud side.

## In progress
- Psalm cleanup (remove redundant casts without changing behavior).
- Verifying routes/request wiring for whoami + HMAC path testing.

## Next
- Fix Psalm warnings in `lib/Service/WeatherApiClient.php` (~lines 144–146) and rerun:
  - `php vendor/bin/psalm --no-cache --threads=1 --config=psalm.xml`
- Run an end-to-end call from Nextcloud → DRF `whoami` using the HMAC scheme and confirm expected response shape + status handling.

## Blockers
- Psalm `RedundantCast` warnings (static analysis gate).
- DRF env/config must be valid for full handshake tests (`WHOAMI_PATH`, valid JSON for HMAC clients list).

## Links / Evidence
- Psalm output:
  - `lib/Service/WeatherApiClient.php:144:34` RedundantCast (string)
  - `lib/Service/WeatherApiClient.php:145:32` RedundantCast (string)
  - `lib/Service/WeatherApiClient.php:146:46` RedundantCast (int)

