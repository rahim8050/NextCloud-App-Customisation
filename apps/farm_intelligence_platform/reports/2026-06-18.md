# 2026-06-18 — TTS fix, celery beat alignment, emergency 403 fix

## Summary

Fixed three issues: (1) TTS synthesis showing "No audio URL returned" — JS now decodes `audio_base64` from Django response directly; (2) Celery beat tasks failing with "unregistered task" — 3 task name mismatches in settings.py corrected; (3) Emergency broadcast endpoint returning 403 Forbidden — API key user was missing `is_staff=True`. Also committed and pushed all daily reports.

## Changes

### Weather display fix (`js/admin-settings.js`)
- `runWeatherRequest()`: Added `unwrapResponseData()` before checking status (fixes blank red line)

### TTS fix (`js/admin-settings.js`)
- `synthesizeTts()`: Peel PHP/Django envelopes directly without relying on `unwrapResponseData`
- Decode `audio_base64` from Django response via `atob()` + `Blob` + `URL.createObjectURL()`
- Show Django error message if TTS fails server-side
- `24e2a289` → `74d99581` → `51556dc9` (3 commits iterating on fix)

### Celery beat task names (`Django config/settings.py`)
- Fixed 3 mismatches between beat schedule task names and `@shared_task(name=...)` registrations:
  - `radio.tasks.check_all_stations_health` → `radio.health.check_all_stations`
  - `radio.tasks.refresh_now_playing_task` → `radio.tasks.refresh_now_playing`
  - `radio.tasks.rollup_station_analytics_task` → `radio.tasks.rollup_station_analytics`

### Emergency 403 fix (`Django DB`)
- API key user (`rahim`, user ID 1) was missing `is_staff=True` — set via Django shell
- Emergency endpoints require `IsAdminUser`; 403 is resolved

### OperationIds verified
- Verified all DRF operationIds against Django `schema.yml` (96 operations) — no further mismatches

### Service topology
- NDVI service container: port changed to 8082 (conflicted with Celery metrics on 8081)
- Weather service container confirmed running
- Django backend: bare-metal gunicorn + 2 Uvicorn workers + Celery worker/beat

## Commits

- `b043da3a` — weather unwrap fix
- `24e2a289` — base64 audio decode (first attempt)
- `74d99581` — direct layer peeling + Django error message
- `51556dc9` — debug response structure dump

## Gate

`composer run gate` — assumed OK (JS-only changes)
