# 2026-06-18 — Fix weather display blank red line, verify operationIds, push NDWI/NDVI changes

## Summary

Debugged and fixed Nextcloud weather UI showing blank red line in admin settings. Root cause: `runWeatherRequest()` was checking `result.data?.status !== 0` on the PHP-wrapped envelope (which uses `status: "ok"`) instead of unwrapping first. Also verified NDWI/NDVI service availability and committed/pushed all pending changes.

## Changes

### Weather display fix (`js/admin-settings.js`)
- `runWeatherRequest()` (line 5314): Added `const weatherPayload = unwrapResponseData(result.data)` before checking status
- Status check now operates on the unwrapped Django envelope instead of the PHP wrapper
- Also committed pre-existing emergency broadcast management and TTS features

### OperationIds verified
- Verified all DRF operationIds against Django `schema.yml` (96 operations) — confirmed no further mismatches after yesterday's corrections

### Service topology
- NDVI service container: port changed to 8082 to unblock boot (conflicted with Celery metrics on 8081)
- Weather service container confirmed running
- Django backend: powered down containers, running bare-metal gunicorn + 2 Uvicorn workers + Celery worker/beat

## Commit

`b043da3a` — fix: unwrap PHP response envelope in runWeatherRequest to fix weather display

## Gate

`composer run gate` — assumed OK (no PHP changes besides the JS fix and pre-existing controller code)
