## Daily Progress — 2026-01-22 (EAT)

### Done
- Investigated Farms admin UI rendering issues (buttons-only table + blank Create/Edit modals).
- Confirmed DRF is healthy and reachable:
  - `GET /api/v1/farms/` → 200
  - `GET /api/v1/farms/19/` → 200
  - `GET /api/schema/?format=json` → 200
  - Token minting `/api/v1/integrations/token/` → 200
- Identified NDVI Timeseries failure root cause:
  - Nextcloud correctly calls DRF timeseries endpoint.
  - DRF returns 400 when Start/End are not provided.
  - Confirmed by DRF logs: `GET /api/v1/farms/19/ndvi/timeseries/` → 400.
- Triggered and reviewed Codex changes for Farms UI:
  - Improved DRF schema extraction for farms (handles `$ref` + paginated `results.items`).
  - Surfaces `fields`, `fieldsCreate`, `fieldsUpdate`, and `columns` from schema.
  - Updated admin UI wiring to prefer schema columns and dedicated create/edit field sets.
  - Added explicit UI guardrails when writable fields/columns are empty.
  - Added regression tests for schema parsing + empty-schema handling.
  - Ran app gates successfully (lint, cs, psalm, phpunit).

### In Progress
- E2E validation of Farms schema endpoint via terminal curl:
  - Found unauthenticated curl returns `Current user is not logged in` (expected for admin endpoints).
  - Need authenticated request capture (“Copy as cURL” from browser Network tab) to validate schema payload end-to-end.

### Next
- E2E verify Farms admin:
  - Confirm `/api/v1/admin/farms/schema` returns non-empty `fields/columns/fieldsCreate/fieldsUpdate` when authenticated.
  - Confirm Farms table renders real columns/values and Create/Edit modals render inputs.
- Wire NDVI UI validation:
  - Require Start/End for Timeseries and raster date for queue/png.
  - Format dates to ISO `YYYY-MM-DD` before calling DRF.
  - Improve proxy error mapping so DRF 4xx surfaces as `invalid_argument` (not `backend_unavailable`).
- Rebuild JS bundle if needed (depending on pipeline) and hard-refresh UI cache.

### Blockers
- Authenticated curl needed for true E2E verification of admin schema endpoint output.
- JS bundling step may be required to ensure the browser is serving updated `js/admin-settings.js`.

### Links / Evidence
- DRF logs: farms list/detail 200, schema 200, token mint 200.
- DRF timeseries 400 confirmed when Start/End missing.
- Nextcloud log showed correct outbound URL for timeseries and `ClientException` on 4xx.

