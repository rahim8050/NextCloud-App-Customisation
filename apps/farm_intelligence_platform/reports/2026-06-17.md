# 2026-06-17 — Verified operationIds against real Django schema

## What was done

Verified every guessed DRF operationId against the actual `schema.yml` from the Django backend (`~/projects/Farm-Intelligence-Platform/schema.yml` — 96 operations total). The old `openapi.json` was stale (only 35 ops); the YAML schema is the correct source.

## Key findings

### 1. Schema copied & operationIds corrected

**File:** `resources/openapi.yml` — replaced with the real `schema.yml` from Django.

**`lib/Service/DrfSchemaService.php`:**
- `ndvi_ingest`: `v1_ndvi_ingest_create` → `v1_ndvi_create` (the actual DRF operationId)

**6 controller `OPERATION_IDS` constants updated** to match the real schema:

| Controller | Changes (old → new) |
|---|---|
| **AdminAlertsController** | `v1_alerts_alerts_admin_send_create` → `v1_alerts_admin_send` |
| **AdminApiKeysController** | `v1_keys_list` → `v1_keys_retrieve` |
| **PodcastsController** | `v1_podcasts_episodes_stream_retrieve` → `v1_podcasts_episode_stream`; `v1_podcasts_refresh_create` → `v1_podcasts_refresh` |
| **UserAlertsController** | `v1_alerts_subscriptions_destroy` → `v1_alerts_subscriptions_delete`; `v1_alerts_alerts_list` → `v1_alerts_list`; `v1_alerts_alerts_retrieve` → `v1_alerts_retrieve` |
| **RadioUserController** | `v1_radio_favorites_destroy` → `v1_radio_favorites_delete`; `v1_radio_history_recent_list` → `v1_radio_history_recent`; `v1_radio_history_stop_create` → `v1_radio_history_stop`; `v1_radio_stations_signed_stream_retrieve` → `v1_radio_stations_stream_signed` |
| **AdminRadioController** | 10 operationIds corrected (stream, now-playing, analytics, health, emergency CRUD, TTS) |

### 2. Missing endpoints in schema (Nextcloud-side routes with no Django operationId)

These endpoints exist in the Nextcloud controllers but have NO matching operationId in the Django schema:

- `GET /api/v1/alerts/subscriptions/{sub_id}/` — `SubscriptionDetailView` has only PATCH+DELETE, no GET
- `UserAlertsController::getSubscription()` — will 405 at runtime

### 3. Gate result

`composer run gate` — **OK** (lint + cs:check + psalm + 202 tests / 1122 assertions).

## Remaining work

- [ ] Fix `$query` uninitialized variable in `AdminFarmsController` catch blocks (used in log before assignment)
- [ ] Remove or stub `getSubscription` — Django backend has no GET endpoint for subscription detail
- [ ] Factor shared helper methods (filterBodyParams, stripPathParams, etc.) into a trait or base class
- [ ] Decide user-identity propagation strategy: `X-Nextcloud-User-Id` header vs client-supplied `external_id`
