# 2026-06-20 — Radio health check fixes + CSS overlap fix

## Summary

Fixed radio health check (custom User-Agent, GET fallback on HEAD exception). Restarted Celery worker (was running old code). Seeded 90 Radio Browser stations. Moved timeout field inside radio section div, added hr separator to fix devallow overlap. Added overflow fix for health cards.

## Changes

### Radio health probe
- `radio/services.py` (Django): Added `User-Agent: FarmIntelligencePlatform/1.0` to `probe_station`. GET fallback when HEAD raises exception.
- Celery worker restarted — was holding old code in memory, reverting `is_available` to `False` every 5 minutes.

### Radio Browser station seeding
- `radio/management/commands/load_stations.py` (Django): Fetch top 100 from Radio Browser API. Field truncation for DB column limits. Country name normalisation (UK → United Kingdom, USA → United States, etc.).

### CSS / template fixes
- `templates/settings/admin.php`: Moved "Timeout seconds" field inside radio section `<div>` so the fixed player bar doesn't overlap it. Added `<hr>` separator between radio section and devallow checkbox.
- `css/admin-settings.css`: Added `overflow: hidden; word-break: break-word` to health cards.

## Stats

- 102 stations total (90 Radio Browser + 8 SomaFM + 3 BBC + 1 TuneIn)
- 89 available, 15 unavailable, 0 unchecked
- App version: 0.0.5
