## Daily Report — Nextcloud App (apps/farm_intelligence_platform) — 2026-01-17

### Done
- Implemented admin diagnostics flow:
  - Mints token server-side (never exposes token to browser)
  - Calls DRF status endpoint
  - Proxies DRF PNG preview through Nextcloud to the UI
  - Retries once on 401 by clearing cached token and re-minting
- Wired new admin routes:
  - `GET /apps/farm_intelligence_platform/admin/diagnostics`
  - `GET /apps/farm_intelligence_platform/admin/preview.png`
- Updated Admin UI with “Run diagnostics” section + preview image.
- Added/updated unit tests for controller, client, settings.
- Fixed production-breaking issue: Nextcloud runs as `www-data` and autoload/DI failed due to unreadable PHP files; corrected ownership/permissions so endpoints stop 500’ing.
- Verified in UI:
  - Test connection ✅ (`expires_in=300s`)
  - Diagnostics ✅ (Status 200, PNG 200, preview loads)

### In progress
- Update `AGENTS.md` with the “www-data readability/permissions” guardrail (Codex task).

### Next
- Codex: merge + apply the super prompt to update `AGENTS.md` (permissions defaults + quick verification commands).
- Optional: add a short note about “HTML error page == server 500” and checking `nextcloud.log` by `reqId`.

### Blockers
- None.

### Links
- Routes: `/apps/farm_intelligence_platform/api/v1/admin/test-connection`, `/apps/farm_intelligence_platform/admin/diagnostics`, `/apps/farm_intelligence_platform/admin/preview.png`
- Key files: `lib/Controller/AdminConfigController.php`, `lib/Service/WeatherApiClient.php`, `js/admin-settings.js`
- Logs: `/var/www/html/nextcloud/data/nextcloud.log`

